// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.

/*$(document).ready(function () {
            
    $(".create").on("click", function (event) {


        function ShowPopup()
    });     
            
            
            

});

function ShowPopup() {
    window.open('/Schedules/Create_Jhonny', "PopupWindow", 'width=640px,height=480px,top=150,left=280');
}


        $("#").on("click", function (event) {

          
        });

    function ShowPopup() {
        window.open('/Schedules/Create_Jhonny', "PopupWindow", 'width=400px,height=480px,top=150,left=280');
    }



        function getSelectedDate() {
            let field = document.querySelector('#data')
            let date = new Date(`${field.value}T00:00`)
            var day = ("0" + date.getDate()).slice(-2);
            var month = ("0" + (date.getMonth() + 1)).slice(-2);
            var today = (day) + "/" + (month) + "/" + date.getFullYear();
            $('#AppointmentDate').val(today);
        }

        function setCurrentDate() {
            var now = new Date();
            var day = ("0" + now.getDate()).slice(-2);
            var month = ("0" + (now.getMonth() + 1)).slice(-2);
            var today = now.getFullYear() + "-" + (month) + "-" + (day);
           return today;
        }

        function getDate() {
            let field = document.querySelector('#AppointmentDate');
            var today;
            // Handle date changes
            field.addEventListener('input', function () {

                // Get the date
                let date = new Date(`${field.value}T00:00`);
                var day = ("0" + date.getDate()).slice(-2);
                var month = ("0" + (date.getMonth() + 1)).slice(-2);
                today = (day) + "/" + (month) + "/" + date.getFullYear();
                console.log(today);

            });
            return today;
        }
*/

